fragment Items on SizedField {
  items {
    id
  }
}

query TestQuery($costlyInput: InputTypeWithCost) {
  listSizePath(paging: { somethingWithCost: 5 })
  listSizeSubselection(paging: $costlyInput) {
    ...Items
  }
}
